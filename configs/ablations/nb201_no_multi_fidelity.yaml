# Phase 4 ablation (NAS-Bench-201 track): every architecture is queried directly at
# full_hp, skipping the screen_hp pairing-and-promotion step.
# mock_mode is forced false — this must be a real LLM-driven run to mean anything.
agent:
  mock_mode: false
nasbench201:
  screening_enabled: false
experiment:
  name: ablation_no_multi_fidelity
