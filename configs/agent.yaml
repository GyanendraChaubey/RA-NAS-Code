llm:
  provider: deepseek
  model: deepseek-chat
  base_url: https://api.deepseek.com
  temperature: 0.7
  max_tokens: 1024
  api_key_env: DEEPSEEK_API_KEY

agent:
  max_iterations: 10
  top_k_memory: 5
  retry_on_invalid: 3
  feedback_strategy: top_k
  mock_mode: true

