torch>=2.1.0
torchvision>=0.16.0
openai>=1.10.0
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.65.0
pytest>=7.4.0
python-dotenv>=1.0.0

# Optional: only needed for the NAS-Bench-201 secondary benchmark track
# (scripts/run_nasbench201.py). Also requires downloading the NATS-tss
# benchmark file — see README's "NAS-Bench-201 track" section.
nats_bench>=1.6

